# define an empty class Foo: """class for foo stuff"""

# instantiate the empty class a = Foo()

# add attributes on the fly (a.b = "foo")

# __init__ function (python passes 'self' silently)
# print ("constructed Foo")

# __init_ function mandatory parameters

# __init_ function default parameters

# inheritance class Bar(Foo): (set defaults in init)

# delete and __del__
